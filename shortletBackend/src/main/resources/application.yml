spring:
  datasource:
    url: jdbc:h2:mem:testdb
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${email}
    password: ${emailPassword}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

paystack:
  secret_key: ${paystackKey}

